MAIN STABLING CARD POLISH

- Added a 6 px coloured status rail on the left of every stabling card
  that has a maintenance/request remark.
- Cards with multiple remarks use stacked rail colours so each status
  remains visible.
- Replaced bright full-card request borders and gradients with the same
  calm steel-blue surface used by normal occupied cards.
- Kept request remark text in its existing status colour.
- Kept all normal and request train numbers bold and pure white in dark mode.
- Kept duplicate train styling unchanged.
- Changed the depot title count from parenthesised text to a compact count badge.
- Added a matching clean light-mode card treatment while retaining the 6 px rail.
- No stabling, maintenance, search, edit, copy, PDF, or clear logic was changed.
